version: "3"

services:
  api:
    build: api
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - DATA_DIR=/data
  
  frontend:
    build: frontend
    ports:
      - "3000:3000"
    depends_on:
      - api